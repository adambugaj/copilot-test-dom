{
  "name": "astro-portal-seo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-sitemap.js",
    "preview": "astro preview",
    "format": "prettier --write ."
  },
  "dependencies": {
    "astro": "^3.0.0",
    "rss": "^1.2.2"
  },
  "devDependencies": {
    "prettier": "^2.8.0"
  }
}